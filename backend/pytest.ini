[pytest]
# Make the `app` package importable when running `pytest` from backend/.
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    # SQLAlchemy/pydantic deprecations are out of scope for these tests.
    ignore::DeprecationWarning
